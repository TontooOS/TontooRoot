# Load partition table and file system modules.
insmod part_gpt
insmod part_msdos
insmod fat
insmod iso9660
insmod ntfs
insmod ntfscomp
insmod exfat
insmod udf

if loadfont "${prefix}/fonts/unicode.pf2"; then
  insmod all_video
  set gfxmode="auto"
  terminal_input console
  terminal_output console
fi

insmod serial
insmod usbserial_common
insmod usbserial_ftdi
insmod usbserial_pl2303
insmod usbserial_usbdebug

if serial --unit=0 --speed=115200; then
  terminal_input --append serial
  terminal_output --append serial
fi

if [ "${grub_platform}" == 'efi' ]; then
  archiso_platform='UEFI'
elif [ "${grub_platform}" == 'pc' ]; then
  archiso_platform='BIOS'
else
  archiso_platform="${grub_cpu}-${grub_platform}"
fi

default=archlinux
timeout=0
timeout_style=hidden

menuentry "Arch Linux (%ARCH%, ${archiso_platform})" --class arch --class gnu-linux --class gnu --class os --id 'archlinux' {
  set gfxpayload=keep
  linux /%INSTALL_DIR%/boot/%ARCH%/vmlinuz-linux archisobasedir=%INSTALL_DIR% archisosearchuuid=%ARCHISO_UUID% cow_spacesize=4G console=ttyS0,115200 quiet splash loglevel=3 rd.udev.log_level=3 vt.global_cursor_default=0 nowatchdog plymouth.ignore-serial-consoles init=/usr/bin/launchpad-daemon --services-dir /System/services
  initrd /%INSTALL_DIR%/boot/%ARCH%/initramfs-linux.img
}

menuentry 'System shutdown' --class shutdown --class poweroff {
  echo 'System shutting down...'
  halt
}

menuentry 'System restart' --class reboot --class restart {
  echo 'System rebooting...'
  reboot
}
