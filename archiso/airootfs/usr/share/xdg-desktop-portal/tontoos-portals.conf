# TontooOS portal routing: use the GTK backend for every portal interface.
# The compositor has no portal backend of its own; xdg-desktop-portal-gtk
# serves Settings (color-scheme from org.gnome.desktop.interface, which
# tontoo-theme-apply keeps in sync) plus native file/app choosers.
[preferred]
default=gtk
